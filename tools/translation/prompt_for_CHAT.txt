You are translating a dataset used for training and evaluating a temporal reasoning model (TISER).

You will receive a JSON ARRAY of examples.
Your task is to translate the content from English to Italian.

IMPORTANT CONSTRAINTS (FOLLOW STRICTLY):

1. JSON STRUCTURE (CRITICAL)
   - The output MUST be a VALID JSON ARRAY.
   - Use ONLY standard ASCII double quotes: ".
   - Do NOT use smart quotes (“ ” ‘ ’) or any other typographic quotes.
   - Do NOT use markdown.
   - Do NOT add any text before or after the JSON.
   - The output must be directly parsable by json.loads().

2. STRUCTURE PRESERVATION
   - Preserve the JSON structure EXACTLY.
   - Do NOT add or remove fields.
   - Do NOT rename keys.
   - Do NOT change the order of elements in the array.
   - Do NOT change the order of keys inside each object.

3. WHAT MUST BE TRANSLATED (MANDATORY)
   You MUST translate ALL natural language text in the following fields:

   - "question"
   - "output"
   - "answer"

   This rule applies EVEN IF:
   - the answer is an entity or event name
   - the answer appears verbatim in the context
   - the answer is copied inside <answer> tags in the output

   If "answer" contains text in English, it MUST be translated to Italian.

4. WHAT MUST NOT BE TRANSLATED
   - field names
   - question_id
   - dataset_name
   - dates, years, numbers
   - proper names of people and places (keep names, translate verbs around them)
   - the "context" field MUST remain EXACTLY as in the input (keep it in English)

5. SPECIAL TAGS (MUST BE PRESERVED EXACTLY)
   Do NOT modify, translate, or reformat:
   <reasoning>, </reasoning>
   <timeline>, </timeline>
   <reflection>, </reflection>
   <answer>, </answer>

6. JSON STRING SAFETY (CRITICAL)
   - Inside ANY JSON string value, you MUST NOT introduce unescaped double quotes (").
   - When referring to events or entities inside text, use single quotes '...' instead of "..."
   - This rule is mandatory for JSON validity.

7. STYLE REQUIREMENTS
   - Use natural, fluent Italian.
   - Do NOT simplify, paraphrase, or summarize.
   - Keep the same level of detail and reasoning.
   - Preserve temporal expressions faithfully.

8. FINAL CONSISTENCY REQUIREMENT (CRITICAL)
   For each example:
   - The text inside the <answer> tag in "output"
     AND the value of the "answer" field
     MUST be identical and MUST be in Italian.

   If they differ, FIX THEM.

9. OUTPUT REQUIREMENTS
   - Return ONLY the translated JSON ARRAY.
   - No explanations.
   - No comments.
   - No markdown.
   - No surrounding text.

Translate ALL samples in the array.
Below is the JSON chunk to translate: